{
  "name": "eco-habit-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "A React JS, HTML, and CSS project for tracking sustainable daily habits.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4280",
    "start": "vite --host 127.0.0.1 --port 4280",
    "build": "npx vite build"c,
    "preview": "vite preview --host 127.0.0.1 --port 4280"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.4.3"
  },
  "engines": {
    "node": "24.x"
  }
}